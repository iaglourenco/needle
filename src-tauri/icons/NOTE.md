Ícones aqui são placeholders (quadrado 1x1 transparente). Assim que Rust/tauri-cli estiverem instalados, gerar os ícones reais com:

```
npm run tauri icon caminho/para/logo-1024.png
```

Isso sobrescreve estes arquivos e gera `icon.ico` (necessário pro bundle Windows nsis/msi).
